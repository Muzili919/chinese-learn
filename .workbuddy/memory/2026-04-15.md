# 2026-04-15 工作日志

### 紫柴犬图片补全（commit 2a0acf7）
- Stage1: 7→9张 (新增excited+normal) ✅ 9/9完整
- Stage2: 9/9 完整 ✅
- Stage3: 4→8张 (新增angry+eating+wave+excited)
- 唯一缺: Stage3 normal.png → 代码fallback到happy
- 全部已处理：去底色裁logo缩放1024px透明RGBA

---

## 紫电柴犬新增（commit e77cef8）
### 🐶 紫电柴犬 (pet_shiba, N级)
- 赛博机械风格：幼崽义体 → 装甲强化 → 终极义体
- 3阶段PNG: Stage1=7张 / Stage2=9张完整 / Stage3=4张
- 缺少: S1缺excited+normal, S3缺angry+eating+wave+excited+normal
- spritePrefix='shiba', 图片在 public/pets/shiba/{stage1,stage2,stage3}/

### 透明背景修复（重要！）
**根因**: ChatGPT导出时"透明背景"被转为RGB+灰白色底(236,238,237)
**修复**: 
- 写了 scripts/process_pet_images.py 批量处理脚本
  - 从四角采样检测背景色
  - RGB→RGBA转换（距离阈值法，带平滑过渡）
  - 裁剪顶部AI logo区域(~120px)
  - 裁切透明边框 + 缩放到1024px
- **小橘猫**: 从精灵图(1222×1287)切分3×3网格 → 去底色 → 27张透明RGBA PNG
- **紫柴犬**: 原图2048×2048去底色裁logo → 20张透明RGBA PNG

### 抽卡权重更新（3只宠物）
| 宠物 | early (Lv1-9) | mid (Lv10-19) | late (Lv20+) |
|------|-------------|--------------|-------------|
| 🐱 小橘猫 | **55%** | 40% | 25% |
| 🐶 紫柴犬 | **30%** | 30% | 25% |
| 🐉 无牙仔 | 15% | 30% | **50%** |

---

## 点击互动修复（commit 9150f66）

### 问题1: Dock/互动页点击宠物不切换表情
**根因**: Pet.handleClick只触发内部happy(1.5s自动恢复normal)，pose prop优先级高于internalPose，所以Dock传的pose永远不变
**修复**: 
- Pet组件新增 `onTap` prop回调，点击时通知父组件
- GlobalPetDock: onTap → handlePetTap → dockPose循环切换（不再依赖Pet内部状态）
- MV1Demo: 新增 interactTapCount 状态 + INTERACT_POSE_SEQUENCE

### 问题2: 动作序列只有6个，缺少eating/reading/normal
**修复**: DOCK_POSE_SEQUENCE 和 INTERACT_POSE_SEQUENCE 都扩展到9个：
`reading → sleeping → happy → wave → excited → angry → sad_cry → eating → normal → 循环`

---

## 大版本更新（commit ff77469）— 头像PNG化+互动+蛋态修复

### 1. 所有头像改为AI精美PNG（6个组件）
| 组件 | 改动 | 尺寸 |
|------|------|------|
| PetSwitchPanel | 顶部大图+列表小图 | 72px / 46px |
| GachaAnimation | 抽卡揭晓 | 140px |
| PetDrawPanel | 已拥有图标+结果展示 | 32px / 64px |
| PetPanel | 主图+切换按钮 | 64px / 24px |
| GlobalPetDock | 最小化按钮 | 26px |

全部使用 PetSpriteAvatar 组件 → public/pets/kitten/ PNG图

### 2. Dock点击互动循环
```
sleeping(默认) → happy(点1) → wave(点2) → excited(点3+) → angry(点4+) → sad_cry(点5+) → 循环
```
- 答题页不受影响：MV1Demo传pose="reading"，强制读书姿态

### 3. 蛋态Bug修复（commit fb402e3 — 第二次彻底修复）
**根因**: MV1Demo.jsx 初始化 currentPet 合并有缺陷
- 原代码: `cloud?.currentPet ? 云端 : base.currentPet(null)` 
- base = initGamificationState() → currentPet=null, ownedPets=[]
- 抽卡后切页面 → MV1Demo重挂载 → 云端可能未及时保存 → 变null=蛋态
**修复**: 三级优先级 — 云端 > initialState(本地) > null(蛋态)
- localHasPet && !cloudHasPet 时保留本地 currentPet + ownedPets
- ownedPets 同样应用合并策略

---

## 之前的完成工作

### 小橘猫PNG表情系统（27张AI精美插图，3阶段×9动作）
- Stage1(1-9级): 幼年橘猫, 无眼镜
- Stage2(10-19级): 戴眼镜少年橘猫  
- Stage3(20-40级): 金色光环魔法阵完全体
- KITTEN_PNG_EMOTIONS映射 + resolveEmotion重构
- 文件: public/pets/kitten/{stage1,stage2,stage3}/ 各9张

### 英语听力题修复（20道T/F→ABCD）
- questions_en_listen.json: 10道
- questions_en_j2_listen.json: 10道

### 宠物系统精简为2只
- 小橘猫(N) + 无牙仔(SR)
- 卖宠物功能(重复→0.5抽卡券碎片)
- 上帝模式(?god=1)

### 白屏修复
- EnglishQuizPage.jsx 第796行正则括号未闭合 → SyntaxError

### 跨设备数据同步（commit 42aa1e3）
**问题**: 同账号换设备后，等级/错题/做题记录全部丢失
**根因**: sync.js 有完整 push/pull，但3个页面未调用 + App.jsx 未拉取
**修复**:
1. App.jsx 用户登录后自动 `pullFromCloud()` 拉取学习数据
2. LightningQuizPage / DictationPage / SelfTestPage 新增 `syncAfterSession()`
3. 现在全部7个答题页面都有云同步了

### 哭哭图片确认（无需代码修改）
**结论**: resolveEmotion 返回 'sad_cry' 时，各宠物已正确映射到自身PNG

### 银月狐PNG裁剪修复（commit f65c256）
**问题**: 9张fox/stage1/*.png全部是从AI精灵图sheet中裁剪出来的，每张都含两行内容（上面完整狐狸+下面下一行的半个头/黑边），原始尺寸739x1024
**修复**: Python PIL自动检测alpha通道内容区域，裁剪掉下半部分多余内容
**结果**: 9张图分别裁去281-515px不等，现在每张只保留完整的狐狸本体

### 紫电柴犬显示无牙仔Bug修复（commit b7d18fc）
**问题**: 用户截图显示"紫电柴犬·幼年·Lv.7"但渲染出的是黑色龙（pet_toothless）
**根因**: Pet.jsx 的 onError 回退机制 → PNG加载失败时回退到 ALL_SPRITES（全是无牙仔龙图）
**修复**:
1. 有PNG系统的宠物(onError)不再回退到龙图，而是用同宠物的其他表情图fallback
2. 增强错误日志输出(type/emotionKey/level/imgSrc)方便后续排查
3. MV1Demo内容区添加 alignItems:center 修复页面偏左
4. 补齐 shiba stage3/normal.png (从happy.png复制)

### 三联修复（commit 21d6410）

**Bug1: 柴犬图片空白**
- 根因: PET_SPRITE_MAP.pet_shiba.emotionSprites 中路径写的是 `/pets/shiba/{stage}/reading.png`，{stage}是字面量不是变量
- 结果: URL变成 /pets/shiba/%7Bstage%7D/reading.png → 404 → 图片完全空白
- 修复: emotionSprites改为 makeShibaStage('stage1') 动态生成真实路径

**Bug2: 切换账号宠物绑定**
- 根因: App.jsx handleOnboarding/handleLogout 未重置 gameState
- 结果: 用户A的宠物数据直接继承给用户B（不重新抽卡）
- 修复: 切换账号和退出时都调用 initGamificationState() 重置为初始

**Bug3: 切换账号后数据不同步**
- 根因: useEffect([user]) 已有 pullFromCloud，但 game state 没重置导致缓存污染
- 修复: 同 Bug2 的 gameState 重置 + pullFromCloud 在 user 变化时自动触发拉取云端等级/XP/连续天数/错题/学习记录

### AI判分+题目修复（commit 51c23d6）

**1. AI判分太慢 → 加速优化**
- callDeepSeek加AbortController 25s超时（原无限等待）
- AI评分+AI分析从串行await改为Promise.allSettled并行
- 预计判分时间缩短约40-50%

**2. vocab_024 幕和燥填空题错误**
- 原题: fill_blank手动输入6个字，手机端体验差+答案格式难匹配
- 拆为: vocab_024(暮) + vocab_024b(燥)，各为3选项single_choice

**3. vocab_054 端的意思缺字**
- 原题: fill_blank要求填序号①-⑤，但前端无序号选项可点
- 改为: single_choice，5个选项对应①-⑤，选"妈妈端来一碗热汤"

### 政治AI评分加速优化（commit 1dea66a）
**问题**: 用户反馈政治AI评分等待时间太长
**根因分析**:
1. `ai_v2.js callDeepSeek()` 无超时控制 → 网络慢时无限等待
2. `PoliticsQuizPage` 材料分析题用 for 循环逐道串行评分 → 3小问=3倍时间
3. max_tokens 无限制 → AI可能生成超长响应拖慢速度

**修复**:
1. `callDeepSeek()` 加 AbortController 25秒超时（全局影响所有学科）
2. max_tokens 默认限制 1024
3. `handleFinalSubmit()`: for 循环串行改为 Promise.allSettled 并行
4. handleFinalSubscribe 添加 finally 确保loading重置

### 四联修复（commit eccddf7）

**Bug1: iOS切换账号重新抽卡**
- 根因链: App.jsx handleOnboarding()重置gameState为initGamificationState(空/蛋态) → MV1Demo收到空initialState → useEffect([])只执行一次不重跑 → 蛋态触发自动抽卡 → 覆盖云端宠物数据
- 修复:
  - App.jsx handleOnboarding(): 删除gameState重置（让MV1Demo自己拉取云端）
  - MV1Demo.jsx: userId/userName空依赖useMemo改为currentUserId信号
  - useEffect依赖从[]改为[currentUserId]，用户切换时重新fetchMV1State

**Bug2: 商店扣人物经验**
- 根因: handleShopAction用calcLevelProgress(s.exp)取的是人物总经验(答题获得)，买道具会降低人物学习等级
- 修复: 改用(totalXP - petExpConsumed)作为可支配经验，购买时增加petExpConsumed而非减少totalXP

**Bug3: 英语联想星球无打勾**
- 根因: EnglishHomePage.jsx的TAG_TO_PLANET缺少'英语联想':'en_association'映射
- 修复: 添加'英语联想'和'联想星球'两条映射

### 宠物经验不再自动升级（commit 3080a36）
**问题**: 用户要求宠物经验只能通过手动点升级，不需要自动升级
**修复**: feedPet() 函数移除 `while (petExp >= 100) { petExp -= 100; petLevel += 1; }` 自动升级循环
**现在的逻辑**:
- 答题 → 加人物经验(state.exp)，不影响宠物等级
- 喂食(feedPet) → 加宠物经验(pet.exp)，但不升级level
- 用户手动点"升级按钮" → 调用manualLevelUp()检查exp是否达标 → 升级

### 三大系统改动（commit f7030a6）

**1. 喂食不加宠物经验**
- feedPet() 移除 pet.exp += amount
- 喂食只增加饱食度(+15)，不再给经验
- 宠物经验唯一来源：每日任务奖励 + 答题获得

**2. 抽卡概率重构（方案B：累计抽卡次数驱动）**
- 旧: 按人物等级分三档(early/mid/late)
- 新: 按累计抽卡次数(totalDraws)分三阶段
  - base(第1-3次): N=78%, R=12%, SR=10% → 体验基础宠物
  - warmup(第4-10次): N=62%, R=18%, SR=20% → R/SR提升
  - boosted(第11次+): N=45%, R=20%, SR=35% → 怜悯模式
- 保底机制(pity): 连续5次未出SR → 第6次必出无牙仔(SR)
- 出了SR/SSR自动重置pityCount为0

**3. 商店抽卡券功能化**
- 旧: card_draw只是buyItem加库存(inventory.cards+1)
- 新: handleShopAction对card_draw特殊处理 → 直接调用drawCard()
- 点击商店"抽卡券"→ 扣500经验 → 触发抽卡动画 → 显示结果

### 商店宠物经验显示修复（commit e29dc93）
**问题**: 用户反馈商店还是显示人物经验而非宠物经验
**根因链**:
1. handleShopAction逻辑层已正确用petExpConsumed追踪消费 ✅
2. 但MV1Demo.jsx第857行 spendableXP = lp.currentExp（人物等级内经验）❌
3. 这个错误的值传给ShopPanel显示 → 用户看到的"可用经验"是人物的
**修复**:
- spendableXP = petExp (totalXP - petExpConsumed) 
- ShopPanel标题 "⭐ 可用经验" → "🐱 可支配经验"

### 切换账号重新抽卡Bug - 彻底修复（commit 43623b9，第5次尝试）

**问题**: 用户A退出→登录用户B→宠物页面显示蛋态→自动或手动抽卡→覆盖B的云端宠物

**根因链（终于彻底搞清）**:
1. handleLogout() → setGameState(initGamificationState()) = 蛋态
2. handleOnboarding(newUser) → gameState保持旧值（用户B看到用户A的宠物？）
3. MV1Demo useState初始值 = initGamificationState() = 蛋态
4. isEggState(null) = true → 显示PetEgg
5. 用户点抽卡 → 覆盖云端数据

**之前4次修复为什么没成功**:
- commit b7d18fc: 重置gameState但MV1Demo useEffect依赖是['has_pet'/'no_pet']，检测不到切换
- commit 21d6410: handleOnboarding不重置了，但handleLogout还在重置
- commit eccddf7: handleOnboarding设null但handleLogout还是initGamificationState()
- commit f7030a6: 同上

**最终修复（6层防护）**:
1. **App.jsx handleOnboarding/handleLogout**: 都 setGameState(null)
2. **App.jsx <MV1Demo key={user.id}>**: React key机制强制重新挂载
3. **MV1Demo useState**: null而非initGamificationState()
4. **渲染保护**: state===null时显示加载中spinner
5. **可选链**: 所有state.xxx改为state?.xxx
6. **操作拦截**: handleDrawCard在state===null时return s(不变)

**经验教训**: 切换账号问题需要从App层到组件层全链路配合，单改一处永远不够
