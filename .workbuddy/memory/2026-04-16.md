# 2026-04-16 工作日志

## shop-house agent 任务完成
- gamification.js 新增：ROOM_THEMES(5个小屋主题) + GAME_SHOP_ITEMS(3个游戏道具) + TITLE_DEFINITIONS(9个称号+computeUnlockedTitles)
- initGodModeState 扩展：gameState/titles/activeTitle/roomTheme/ownedRoomThemes/gameInventory
- initGamificationState 同步扩展默认值
- ShopPanel.jsx 重构：Tab栏从 道具店/装扮店/我的搭配/背包 → 道具店/小屋装饰/游戏道具/背包
- 删除了配饰相关Tab（accessories/equipped）的完整渲染区块
- 新增小屋装饰Tab（主题预览卡片+2列网格列表）
- 新增游戏道具Tab（横向列表+持有数量+购买/已满）
- 清理了不再使用的import和变量

## 🔥 大功能：单词炮台游戏 + 商店重构 + 称号系统（commit fc8d2e2）

**3个Agent并行开发完成**：

| Agent | 产物 |
|-------|------|
| game-core | WordCannonGame.jsx (~39KB, CSS气泡下落+宠物技能+波次系统) |
| shop-house | ShopPanel重构 + gamification.js数据扩展 |
| leaderboard-titles | 排行榜炮台分数 + mv1_cloud同步 + MV1Demo购买逻辑 |

**功能清单**：
1. **⚔️ 单词炮台游戏**：CSS气泡下落+宠物技能(稀有度差异化)+波次+连击加成+每日次数
2. **🏠 小屋装饰**：5套主题(星空/森林/城市/极光/火山)，替代原装扮Tab
3. **⚡ 游戏道具商店**：额外次数/时间冻结/护盾药水
4. **🏅 称号系统**：9个称号含隐藏「欧皇附体🎰」
5. **🏆 排行榜新增炮台高分维度**

**Bug修复**：game-core的TDZ错误（const phase重复赋值）→ 删除多余行

## 🐱 宠物系统P2大升级 (commit 39b2889)

### 1. PNG透明度修复
- kitten(27张) + shiba(27张) + fox(27张) = **81张图片**从RGB转为RGBA
- 用numpy检测背景色+白色区域，自动去底
- hamster/corgi/butterfly/mantis/squirrel/kungfu/toothless 本身已是RGBA ✅

### 2. 动作状态机v2（核心改造）
**规则优先级（从高到低）：**
- P0 答题模式 → 强制锁定 reading，点击无反馈无切换
- P1 低血量 → 任意属性<10% → 锁定 sad_cry
- P2 连续暴击 → 20次点击显示 angry（3秒后恢复）
- P3 正常互动 → 5~12秒随机切换 / 每3次点击切换动作

**改动文件：**
- Pet.jsx: 新增`isLocked` prop，handleClick中拦截
- MV1Demo.jsx: 完整状态机逻辑（isQuizMode/isLowStat/showAngry/clickCount/currentActionIndex）

### 3. 抽卡展示
- GachaAnimation: level=1固定 → 20~40级随机 + pose=100随机可爱表情

### 4. 售卖系统重做
- sellPet(): 奖励从0.5卡券碎片 → **250宠物成长经验(petExp)**
- UI文案更新为"💰 +250经验"

### 5. PetSpriteAvatar v3
- 支持全部11只宠物（新增7只）
- 新增pose参数：0=reading, 1=happy, 2=normal, 3=excited, >=100=随机可爱

## 📚 阅读理解Batch3生成完成 (reading-batch3 agent)
- **5篇**初中英语阅读理解 (ID: en_j2_read_011 ~ en_j2_read_015)
- **主题**: 11.名人励志(Edison) / 12.安全急救 / 13.志愿者活动 / 14.中国茶文化 / 15.未来职业规划
- **输出**: `src/data/reading_batch3.json` + 脚本 `scripts/gen_reading_batch3.py`
- **质量**: 每篇5题×5篇=25题，ABCD分布 A4/B8/C7/D6 均衡
- **踩坑**: Python 3.14的re.findall对\d和\s有兼容性问题，改用字符串分割做验证

## 📚 阅读理解Batch2生成完成 (reading-batch2 agent)
- **5篇**初中英语阅读理解 (ID: en_j2_read_006 ~ en_j2_read_010)
- **主题**: 6.旅行与文化体验(澳洲交换生) / 7.动物保护与自然(大熊猫) / 8.中外节日对比(春节感恩节排灯节亡灵节) / 9.科技改变生活(智能手机) / 10.环境保护行动(青少年先锋)
- **输出**: `src/data/reading_batch2.json` + 脚本 `scripts/gen_reading_batch2.py`
- **质量**: 每篇5题×5篇=25题，全部验证通过
- **答案分布**: A=2/B=8/C=9/D=6（D从0调整到6）
- **踩坑**: 
  - Python 3.14的`\s+`正则无法匹配普通空格（原因不明），改用显式空格` `匹配答案格式
  - `\([1-9]\s+[ABCD]` 完全不match，但 `\([0-9] [A-D]` 和 `\([1-9]\) ([ABCD])` 正常工作
  - 选项长度差距需控制在1.5倍内（原始Q4的D选项35 vs A/C的22超标）
