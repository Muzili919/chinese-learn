# 2026-04-17

## Bug#4 好友系统全挂修复 (bugfix-sprint)
- **子问题1（刷新后好友消失）**：App.jsx:370-382 云端合并用 `...cloud` 做底，但 friends 不在保留字段列表 → 云端空值覆盖本地好友。修复：新增 `friends: prev.friends?.length > 0 ? prev.friends : (cloud.friends || [])`
- **子问题2（好友信息空白）**：`api/friend-preview.js:44` 用了不存在的 `s.petPool` 查宠物 → 全部 fallback 默认值。修复：import PET_POOL 常量 + 联查 users 表获取 playerName
- **子问题3（排行榜无好友）**：是#1和#2的连锁结果，修好前两个自动解决
- **修改文件**：`api/friend-preview.js` + `src/App.jsx`

## Bug#3 游戏次数深度修复 v2 (bugfix-sprint)
### 第一版修复（局部，不够）
- startGame加守卫+立即扣次、endGame去重扣次
- 老板反馈：刷新和切换账号后仍然失败

### 第二版修复（根因级）
- **致命根因**：MV1Demo.jsx 云端合并时 gameState 没有从 localStorage 优先恢复！
  - gameInventory/ownedFurniture 都用了 `localSnapshot ?? cloud ?? base` 三层优先级
  - 但 gameState 没有，只靠 `...base`(默认dailyAttempts=3) + `...(cloud||{})`
  - 刷新后 localStorage 中的最新 dailyAttempts 直接被丢弃 → 次数"恢复"
- **修复1** MV1Demo.jsx merged对象新增 `gameState: localSnapshot?.gameState ?? (cloud?.gameState || base.gameState)`
- **修复2** WordCannonGame 初始化useEffect兼容两种日期字段名（_wordCannonDate/dailyGameDate）
- **修复3** 首次无数据给满额而非0（新用户应该能玩）
- **教训**：修bug必须追踪完整数据链路，不能只改组件层。storage→state→component 全链路审计才是正道

## 宠物动作规则修复
- **根因**：Pet.jsx `resolveEmotion()` 没有将 `sad_cry`/`angry`/`reading` 加入透传白名单，导致 GlobalPetDock 正确计算的 pose 被四维属性自动判断覆盖
- **修复**：新增 `SYSTEM_FORCED_POSES = ['sad_cry', 'angry', 'reading']` 白名单，优先级最高（P0）
- 数据流：GlobalPetDock计算dockPose(含isLearning/lowStat/angry逻辑) → 传pose给Pet → resolveEmotion直接透传 → getEmotionSprite渲染正确图片
- 同时修复了：政治错题入口缺失、游戏无限次玩、技能提示缺失（commit b2b9636）

## 游戏页面手机端适配 (commit ad2ca1a)
- container: minHeight→100dvh + flex列布局
- gameArea: 固定520px→flex:1自动填充
- 所有固定UI区加flex-shrink:0防压缩
- ResizeObserver动态测量gameArea实际高度

## 新增4只SR级宠物 (commit 578e443)
- 🌸 花瓣精灵(petal_fairy) - 缺wave/eating，7张/阶段
- 🤖 机械翼龙(mecha_dragon) - 9张齐全
- 🔥 小凤凰(phoenix) - 缺normal，8张/阶段
- 🦄 星光小马(starpony) - 缺wave/eating，7张/阶段
- 精灵表裁切脚本: scripts/crop_sr_pets.py（3×3网格→9张PNG + 去白底透明）
- 宠物池从11只扩展到15只

## Bug#7 上帝模式彻底修复 (bugfix-sprint, 08:22 全链路版)

### 根因（全链路审计发现）
**不只是 exp 被覆盖，而是有 4 处交互回调中的 `storage.getXP(null)` 完全没保护**：
1. `handlePetLevelUp` — 宠物升级时读经验 → 返回0 → 不能升级
2. `handleShopAction` card_draw — 买抽卡券 → 返回0 → 买不了
3. `handleShopAction` 主分支 — 商店消费 → 返回0 → 可支配经验显示0
4. `handleDrawCard` — 抽卡 → 返回0 → 抽不了卡

**之前只修了定时器和派生数据（5处），但这4处回调完全漏了 → 用户操作任何功能都碰到0**

### 彻底修复方案：统一 `getEffectiveXP()` 辅助函数
在 MV1Demo.jsx 顶部创建 `getEffectiveXP(state, user)` 统一函数：
- 有正常用户 → 读 `storage.getXP(user.id)`
- 上帝模式无用户 → 用 `state.exp`（initGodModeState 的 99999）
- **替换全部10处散落的 `storage.getXP()` 调用**

### 7b: 配饰无法取消/卸下
ShopPanel 注释写了配饰但从未实现UI，新增「👗装扮」tab完整面板

### 修改文件
- `src/pages/MV1Demo.jsx` — 新增 getEffectiveXP() + 替换10处调用
- `src/components/ShopPanel.jsx` — 新增配饰装扮UI

### 教训：不能头痛医头！必须追踪完整数据流。第一次只改了5处定时器保护，被老板指出后做全链路审计才发现4处交互回调完全遗漏

## Bug#2 宠物经验修复 (bugfix-sprint)
- **根因（两层）**：
  1. `gainExpForLearning` 是死代码（定义了但从未被调用），答题页只调 storage.addXP 不走 gainExpForLearning → currentPet.exp 永远不增加
  2. MV1Demo 答题时被卸载，重新挂载后初始化流程只同步 state.exp（人物经验），不同步 currentPet.exp（宠物经验）
- **修复**（2文件4处）:
  - `gamification.js`: gainExpForLearning 新增 currentPet.exp += petGain（80%比例）
  - `MV1Demo.jsx` 初始化: 根据今日答题记录回填宠物经验 + 防重复机制(localStorage key按天+用户隔离)
  - `MV1Demo.jsx` L1/L2/L3 同步定时器: XP变化时同时更新 currentPet.exp
- **关键**: resolvedCurrentPet 从 const 改为 let（初始化中需要重新赋值）
- 构建验证通过 ✅

## Bug#1 切换账号数据重置修复 (bugfix-sprint, 08:14 → 08:25 全面版)

### 第一轮修复（08:14）— 3处改动
- **根因**：MV1Demo.jsx 第547行 `const currentUserId = storage.getUser()?.id` 在组件顶层渲染时同步调用storage
- **修复**：改为 props `propUserId` 接收 + App.jsx 传入 + handleOnboarding 显式 pullFromCloud

### 第二轮全链路修复（08:23）— 用户要求彻底排查，不再打补丁
**全链路追踪发现5个问题，全部一次性修复：**

| # | 问题 | 文件 | 根因 |
|---|------|------|------|
| A | **persist effect 缺少 currentUserId 依赖** | MV1Demo.jsx:741 | 依赖数组 `[state, onStateChange]` 漏了 `currentUserId`，切换账号后可能写入旧的 localStorage key |
| B | **init effect 内部重新调 storage.getUser()** | MV1Demo.jsx:555-579 | 已有 propUserId 但 effect 内部又 `const user = storage.getUser()`，闭包捕获旧 userId |
| C | **App.jsx gameState 初始化回退到蛋态对象** | App.jsx:330 | `readPetState` 返回 null 时返回 `initGamificationState()`（exp:100, friends:[]），污染 Leaderboard 等消费者 |
| D | **DictationPage 写入 null userId** | DictationPage.jsx:555 | `storage.addXP(null, ...)` → XP 写到 `cl_xp_null` 永远拿不到 |
| E | **App.jsx fetchMV1State 合并逻辑** | App.jsx:370-382 | 已在之前 bug#4 修复时加了 friends 保留 ✅ 无需再改 |

### 最终修改文件清单（4个文件）
1. **MV1Demo.jsx**: 
   - currentUserId 改为 propUserId
   - init effect 全部改用 uid（不再调用 storage.getUser()）
   - persist effect 加上 currentUserId 依赖
2. **App.jsx**: 
   - `<MV1Demo>` 增加 currentUserId prop
   - handleOnboarding 增加 pullFromCloud
   - gameState 初始化 null 而非蛋态对象
3. **DictationPage.jsx**: addXP null → user?.id

### 构建验证：vite build 成功，0错误

## Bug#8 错题集学科分类修复 (bugfix-sprint, 08:22 深度版)

### 发现的全部问题（6个维度排查）
| # | 问题 | 严重度 |
|---|------|--------|
| 1 | QuizPage addRecord 缺少 subject 字段（语文答题不标记学科） | 🔴 高 |
| 2 | WrongAnswersPage useMemo 依赖数组缺 subject（切换学科不刷新） | 🔴 高 |
| **3** | **WrongAnswersPage 内部完全没有学科切换UI！用户无法切换到其他学科查看错题** | **🔴🔴 致命（核心根因）** |
| **4** | **点击"错题"tab时显示哪个学科取决于主页最后选的科目，用户无感知** | **🟠 中** |
| 5 | pullFromCloud 直接覆盖本地records（非本次主因） | 🟡 低 |

### 根因
WrongAnswersPage 是架构设计缺陷的"死页面"：只接收一个 subject prop 就展示该学科，内部没有学科切换、数量角标、状态重置、父组件同步。用户点错题tab后完全无法控制看到的是哪个学科。

### 修改文件
1. **`src/pages/WrongAnswersPage.jsx`** — 核心重写：
   - 新增 `currentSubject` 内部状态 + 学科标签栏 UI（📖语文/🌎英语/⚖️道法 + 错题数角标）
   - 提取 `computeWrongCards()` 纯函数供主显示+计数复用
   - 新增 `onSubjectChange` 可选回调 prop
   - useMemo 依赖补全为 `[user.id, currentSubject, allQMap]`
2. **`src/App.jsx`** — 两处 WrongAnswersPage 调用加 `onSubjectChange={setActiveSubject}`
3. **`src/pages/QuizPage.jsx`** — addRecord 补上 `subject: 'chinese'`

### 教训
不要只修表面（缺字段+依赖），必须追踪完整数据流和用户体验路径。这次第一次只改了2个表面问题，被老板指出后做了6维度深度排查才发现真正的致命缺陷是页面缺少学科切换能力。

### 第二轮深度修复（08:31 最终验证）
**又发现3个遗漏问题：**
1. **SentencePracticePage.jsx** — 造句练习 addRecord 缺 `subject: 'chinese'`
2. **ReadingPage.jsx** — 阅读理解 addRecord 缺 `subject: 'chinese'`
3. **sync.js pullFromCloud** — records/sessions/essays/srs 四项数据直接覆盖本地数据（云端旧数据会覆盖本地新数据），改为并集合并策略

**完整修复清单（6个文件，8个问题）：**
| 文件 | 改动 |
|------|------|
| WrongAnswersPage.jsx | 重写：学科标签栏 + currentSubject状态 + computeWrongCards复用 |
| App.jsx | 两处WrongAnswersPage加onSubjectChange回调 |
| QuizPage.jsx | subject:'chinese' |
| SentencePracticePage.jsx | subject:'chinese' |
| ReadingPage.jsx | subject:'chinese' |
| sync.js | pullFromCloud 4项数据从覆盖→合并 |

**教训²：全量扫描所有addRecord调用点，不能只查QuizPage就收工。**

## Feature#5 作文星球全链路修复 (bugfix-sprint, 08:24)

### 初始问题
EssayPage.jsx 打开即崩溃 `useMemo is not defined`

### 用户反馈"还是不行"后做全链路审计（路由→状态→数据流→storage→AI调用→ResultPage），共6个问题：

| # | 严重度 | 问题 | 修复 |
|---|--------|------|------|
| 1 | 🔴 | useMemo未导入 | 补入import |
| 2 | 🔴 | onFinish跳ResultPage格式不匹配→评分完返回崩溃 | 改onBack() |
| 3 | 🟡 | 切换账号user为null无guard | 加loading spinner |
| 4 | 🟡 | 写作文没XP奖励 | 加+20XP |
| 5 | 🟢 | Progress区每次渲染读localStorage | 改用user prop |
| 6 | 🟢 | 分数Math.round溢出0-10 | 加clamp |

### 文件：仅改 `src/pages/EssayPage.jsx`
### 教训：不能头痛医头，必须全链路审查

## Bug#6 星球打卡彻底修复（第二轮深度修复，08:23）

### 老板反馈
"还是不行，不准撒谎，不准只改局部一个问题！刷新和切换账号之后这个功能就失败了"

### 4个根因（第一轮只修了表面症状）

| # | 严重度 | 根因 | 修复文件 |
|---|--------|------|----------|
| **1** | **🔴🔴 致命** | **云同步完全缺失completedPlanets！sync.js的pullFromCloud和syncAfterSession都没处理打卡数据 → 刷新/切换账号后打卡消失** | `src/utils/sync.js` |
| **2** | **🔴 高** | **SelfTestPage完全缺少markPlanetComplete → 自测星球永远无法打勾** | `src/pages/SelfTestPage.jsx` |
| 3 | 🟡 | 4个页面缺打卡（联想/单词/造句/听写） | 第一轮已修，验证通过 |
| 4 | 🟡 | Tag映射不完整（入口页TAG_TO_PLANET缺项） | EnglishHomePage + PoliticsHomePage |

### 根因1详情（核心）
- pullFromCloud拉取: records/srs/sessions/xp/streak/essays ❌ 没有completedPlanets
- syncAfterSession推送: records/srs/userStats/essays ❌ 没有completedPlanets
- 修复：+pushCompletedPlanetsToCloud函数 + pullFromCloud中拉取并做并集合并 + syncAfterSession加入调用

### 教训
**必须追踪数据从写入→存储→读取→云同步的完整链路。localStorage数据本身不丢，但云同步覆盖会导致数据丢失。只加markPlanetComplete调用而不修云同步 = 治标不治本。**

### 修改文件共7个
sync.js / SelfTestPage.jsx / EnglishHomePage.jsx / PoliticsHomePage.jsx / AssociationPlanetPage.jsx / WordPlanetPage.jsx / SentencePracticePage.jsx / DictationPage.jsx / QuizPage.jsx / ReadingPage.jsx
